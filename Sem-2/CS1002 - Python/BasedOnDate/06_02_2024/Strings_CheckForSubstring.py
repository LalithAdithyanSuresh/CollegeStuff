# Check if a substring is present in a string

Main , sub = input("Enter the main string : "),input("Enter the substring : ")
if Main.find(sub) != -1: print("Found the Substring")
else: print("The substring is not found : (")